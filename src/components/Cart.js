import React, { Component } from 'react'

export default class Cart extends Component {
    render() {
        //get cart to update based on what items are in cart
             //should update the total price, tax, and  total
        return (
            <div>
                
            </div>
        )
    }
}
